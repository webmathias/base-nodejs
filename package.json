{
  "name": "node-tests",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "docker-compose -f docker-compose.test.yml up",
    "test-in-docker":"../node_modules/.bin/mocha --watch tests/*" 
  },
  "devDependencies": {
    "eslint": "^5.1.0",
    "mocha": "^5.2.0",
    "docker-compose": "^0.8.1"
  }
}
